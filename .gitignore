# Virtual environments
venv/
.venv/
env/

# Bytecode and caches
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
coverage.xml
*.cover
htmlcov/
.tox/
.nox/

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/

# Environment and secrets
.env
.env.*
!.env.example

# Editors and OS
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs and local tooling
*.log
.scrapy/

# Frontend (Vite / npm)
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.env
frontend/.env.*
!frontend/.env.example

# TypeScript incremental / Vite
*.tsbuildinfo

# Docker Compose: local overrides (secrets, published ports, bind mounts)
docker-compose.override.yml

# Optional local tooling
.envrc

# Pickle binaries: only `models/*.pkl` is versioned (Git LFS). Ignore stray dumps elsewhere.
*.pkl
!models/*.pkl
